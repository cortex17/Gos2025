# ⚡ СРОЧНО: Как запустить бэкенд ПРЯМО СЕЙЧАС

## 🎯 Быстрое решение

### Вариант 1: Использовать скрипт (РЕКОМЕНДУЕТСЯ)

Откройте **новый терминал** и выполните:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap"
./start-backend.sh
```

### Вариант 2: Запустить вручную

1. Откройте **новый терминал** (не закрывайте его!)
2. Выполните команды:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
source venv/bin/activate
python run.py
```

3. **НЕ ЗАКРЫВАЙТЕ** этот терминал! Бэкенд должен работать в нем.

### Вариант 3: Если не работает - используйте Fake API

1. Создайте файл `.env` в корне проекта `/Users/nurdaulettauekel/Desktop/SOSMap/`:
```env
VITE_USE_FAKE_API=true
```

2. Перезапустите фронтенд:
```bash
npm run dev
```

## ✅ Проверка работы

После запуска бэкенда откройте в браузере:
- http://localhost:8000/docs - должна открыться документация API
- http://localhost:8000/health - должен вернуть `{"status":"ok"}`

## 🔍 Если бэкенд не запускается

### Проблема: Порт занят
```bash
# Убить все процессы на порту 8000
kill -9 $(lsof -ti:8000)
```

### Проблема: База данных
Убедитесь, что PostgreSQL запущен и база данных существует.

### Проблема: Зависимости
```bash
cd "GOS 2025/backend"
source venv/bin/activate
pip install -r requirements.txt
```

## 📝 Важно!

**Бэкенд должен работать в отдельном терминале!** Не закрывайте терминал, где он запущен.

Если вы видите ошибку "Не удалось подключиться к серверу", это значит:
1. Бэкенд не запущен ИЛИ
2. Бэкенд запущен, но на другом порту ИЛИ  
3. Проблема с CORS (но мы это уже исправили)

**Попробуйте сначала запустить бэкенд вручную в отдельном терминале!**

