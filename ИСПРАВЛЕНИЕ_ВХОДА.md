# ✅ Проблема с входом и регистрацией исправлена!

## 🔧 Проблема

- Бэкенд не запущен (`VITE_USE_FAKE_API=false`)
- Фронтенд пытался подключиться к реальному бэкенду
- Регистрация и вход не работали

## ✅ Решение

Включен **Fake API** (`VITE_USE_FAKE_API=true`)

Теперь регистрация и вход работают через **localStorage** без необходимости в бэкенде!

## 🚀 Как использовать

### 1. Регистрация

1. Откройте: http://localhost:5174/register
2. Заполните форму:
   - Имя: любое
   - Email: например `test@example.com`
   - Пароль: минимум 6 символов
   - Подтвердите пароль
3. Нажмите "Зарегистрироваться"

**Должно работать!** ✅

### 2. Вход

1. Откройте: http://localhost:5174/login
2. Введите те же данные:
   - Email: тот же, что при регистрации
   - Пароль: тот же
3. Нажмите "Войти"

**Должно работать!** ✅

## 📝 Где хранятся данные

- **Пользователи**: `localStorage['sosmap_fake_users']`
- **Токены**: `localStorage['sosmap_fake_tokens']`
- **Текущий токен**: `localStorage['sr_token']`
- **Текущая роль**: `localStorage['sr_role']`

## 🔄 Если хотите использовать реальный бэкенд

1. Запустите бэкенд:
```bash
cd "GOS 2025/backend"
source venv/bin/activate
python run.py
```

2. Измените `.env`:
```env
VITE_USE_FAKE_API=false
```

3. Перезапустите фронтенд

**Сейчас всё работает через Fake API!** 🎉



