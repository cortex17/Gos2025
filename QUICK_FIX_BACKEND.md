# 🔧 Быстрое исправление проблемы с бэкендом

## Проблема
Бэкенд не запускается или не отвечает на http://localhost:8000

## Решение

### Шаг 1: Откройте терминал и перейдите в папку бэкенда
```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
```

### Шаг 2: Активируйте виртуальное окружение
```bash
source venv/bin/activate
```

### Шаг 3: Проверьте, что все зависимости установлены
```bash
pip install -r requirements.txt
```

### Шаг 4: Убедитесь, что PostgreSQL запущен
```bash
# Проверьте статус PostgreSQL (macOS)
brew services list | grep postgresql

# Или запустите PostgreSQL
brew services start postgresql
```

### Шаг 5: Проверьте настройки базы данных
Убедитесь, что файл `.env` содержит правильный `DATABASE_URL`:
```
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/saferoute
```

### Шаг 6: Создайте базу данных (если не существует)
```bash
psql -U postgres -c "CREATE DATABASE saferoute;"
```

### Шаг 7: Запустите бэкенд
```bash
python run.py
```

Или используйте скрипт:
```bash
chmod +x run_server.sh
./run_server.sh
```

## Альтернатива: Использовать Fake API

Если бэкенд не работает, можно временно использовать Fake API:

1. Создайте/отредактируйте `.env` в корне проекта:
```env
VITE_USE_FAKE_API=true
```

2. Перезапустите фронтенд:
```bash
npm run dev
```

## Проверка работы бэкенда

После запуска откройте в браузере:
- http://localhost:8000/docs - Swagger документация
- http://localhost:8000/health - Health check

Если видите JSON ответ, бэкенд работает!

