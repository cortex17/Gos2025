# 🚀 Инструкция по запуску бэкенда

## Быстрый запуск

### Вариант 1: Используя скрипт (рекомендуется)
```bash
cd "GOS 2025/backend"
chmod +x run_server.sh
./run_server.sh
```

### Вариант 2: Вручную
```bash
cd "GOS 2025/backend"
source venv/bin/activate
python run.py
```

### Вариант 3: Если `python` не найден
```bash
cd "GOS 2025/backend"
./venv/bin/python run.py
```

## Проверка работы

После запуска бэкенд должен быть доступен на:
- **API**: http://localhost:8000
- **Документация**: http://localhost:8000/docs
- **Health check**: http://localhost:8000/health

## Требования

1. **PostgreSQL** должен быть запущен
2. **База данных** должна существовать (по умолчанию `saferoute`)
3. **Переменные окружения** в `.env` файле настроены

## Проверка подключения к БД

Если бэкенд не запускается, проверьте:
```bash
# Проверьте, что PostgreSQL запущен
psql -U postgres -c "SELECT version();"

# Проверьте, что база данных существует
psql -U postgres -c "\l" | grep saferoute
```

## Устранение проблем

### Порт 8000 занят
```bash
# Найти процесс
lsof -ti:8000

# Убить процесс
kill -9 $(lsof -ti:8000)
```

### Ошибки импорта
```bash
cd "GOS 2025/backend"
source venv/bin/activate
pip install -r requirements.txt
```

### Проблемы с БД
Убедитесь, что в `.env` указан правильный `DATABASE_URL`:
```
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/saferoute
```

