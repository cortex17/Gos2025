# ✅ Рабочие сервисы

## 🚀 Текущий статус

### Фронтенд ✅
- **URL**: http://localhost:5174 (порт 5173 был занят)
- **Статус**: ✅ РАБОТАЕТ
- **Логи**: `/tmp/frontend.log`

### Бэкенд ⚠️
- **URL**: http://localhost:8000
- **Статус**: Запускается...
- **Логи**: `/tmp/backend_final.log`

## 🔧 Если бэкенд не запустился

### Вариант 1: Запустить вручную

Откройте **новый терминал** и выполните:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
source venv/bin/activate
pip install -r requirements.txt
python run.py
```

**НЕ ЗАКРЫВАЙТЕ этот терминал!**

### Вариант 2: Использовать Fake API

Если бэкенд не нужен, используйте Fake API:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap"
echo "VITE_USE_FAKE_API=true" > .env
# Перезапустите фронтенд
```

## 📝 Проверка

### Бэкенд:
```bash
curl http://localhost:8000/health
# Должен вернуть: {"status":"ok"}
```

### Фронтенд:
Откройте: http://localhost:5174

## 🎯 Регистрация

Сейчас фронтенд настроен на реальный бэкенд (`VITE_USE_FAKE_API=false`).

Если бэкенд не работает, временно включите Fake API:
```bash
echo "VITE_USE_FAKE_API=true" > .env
```




