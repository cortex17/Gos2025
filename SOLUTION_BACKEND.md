# 🎯 РЕШЕНИЕ: Проблема с бэкендом

## ❌ Текущая проблема
1. Ошибка `greenlet._greenlet` - библиотека установлена неправильно
2. Бэкенд не запускается
3. Фронтенд не может подключиться

## ✅ РЕШЕНИЕ (выберите один вариант)

### Вариант 1: Исправить greenlet (РЕКОМЕНДУЕТСЯ)

**Откройте новый терминал** и выполните:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
source venv/bin/activate
pip uninstall greenlet -y
pip install --upgrade greenlet
python run.py
```

**НЕ ЗАКРЫВАЙТЕ этот терминал!** Бэкенд должен работать в нем.

### Вариант 2: Переустановить виртуальное окружение

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python run.py
```

### Вариант 3: Использовать Fake API (БЫСТРОЕ РЕШЕНИЕ)

Если бэкенд не нужен прямо сейчас, используйте Fake API:

1. **Создайте `.env` в корне проекта** (`/Users/nurdaulettauekel/Desktop/SOSMap/.env`):
```env
VITE_USE_FAKE_API=true
```

2. **Перезапустите фронтенд**:
```bash
npm run dev
```

Теперь регистрация будет работать через Fake API (данные в localStorage).

## 🔍 Проверка работы бэкенда

После запуска откройте в браузере:
- http://localhost:8000/docs - Swagger документация
- http://localhost:8000/health - должен вернуть `{"status":"ok"}`

## 📝 Важно!

**Бэкенд должен работать в ОТДЕЛЬНОМ терминале!**

Если вы запустили бэкенд и видите:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

Значит бэкенд работает! Теперь регистрация должна работать.

## 🚀 После запуска бэкенда

1. Убедитесь, что в `.env` фронтенда:
```env
VITE_API_BASE_URL=http://localhost:8000
VITE_USE_FAKE_API=false
```

2. Перезапустите фронтенд:
```bash
npm run dev
```

3. Попробуйте зарегистрироваться!

