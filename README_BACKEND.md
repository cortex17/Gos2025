# 🔧 Запуск бэкенда - Пошаговая инструкция

## Проблема: "Не удалось подключиться к серверу"

Если вы видите эту ошибку, значит бэкенд не запущен или недоступен.

## ✅ Решение

### Способ 1: Использовать готовый скрипт (самый простой)

```bash
# В корне проекта SOSMap
./start-backend.sh
```

### Способ 2: Запустить вручную

1. **Откройте терминал** и перейдите в папку бэкенда:
```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
```

2. **Активируйте виртуальное окружение**:
```bash
source venv/bin/activate
```

3. **Запустите сервер**:
```bash
python run.py
```

Или используйте скрипт из папки бэкенда:
```bash
chmod +x run_server.sh
./run_server.sh
```

### Способ 3: Если Python не найден

Используйте полный путь к Python из venv:
```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
./venv/bin/python run.py
```

## 🔍 Проверка работы

После запуска бэкенда:

1. **Откройте в браузере**: http://localhost:8000/docs
   - Должна открыться Swagger документация

2. **Или проверьте health endpoint**: http://localhost:8000/health
   - Должен вернуть: `{"status":"ok"}`

3. **Проверьте корневой endpoint**: http://localhost:8000/
   - Должен вернуть: `{"message":"SafeRoute API"}`

## ⚠️ Возможные проблемы

### Порт 8000 занят
```bash
# Найти процесс
lsof -ti:8000

# Убить процесс
kill -9 $(lsof -ti:8000)
```

### Ошибка подключения к базе данных
Убедитесь, что:
1. PostgreSQL запущен
2. База данных `saferoute` существует
3. В `.env` правильный `DATABASE_URL`

### Ошибки импорта
```bash
cd "GOS 2025/backend"
source venv/bin/activate
pip install -r requirements.txt
```

## 🎯 Временное решение: Fake API

Если бэкенд не работает, можно использовать Fake API:

1. Создайте `.env` в корне проекта:
```env
VITE_USE_FAKE_API=true
```

2. Перезапустите фронтенд:
```bash
npm run dev
```

## 📝 Настройка .env для фронтенда

Убедитесь, что в корне проекта есть `.env`:
```env
VITE_API_BASE_URL=http://localhost:8000
VITE_USE_FAKE_API=false
```

После запуска бэкенда регистрация должна работать! 🎉

