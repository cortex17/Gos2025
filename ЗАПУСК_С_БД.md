# 🗄️ Запуск с базой данных

## ✅ Настройки применены

Фронтенд настроен на подключение к реальному бэкенду:
- `VITE_USE_FAKE_API=false` - используем реальный API
- `VITE_API_BASE_URL=http://localhost:8000` - адрес бэкенда

## 🚀 Запуск бэкенда

### Шаг 1: Откройте новый терминал

### Шаг 2: Перейдите в папку бэкенда
```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
```

### Шаг 3: Активируйте виртуальное окружение
```bash
source venv/bin/activate
```

### Шаг 4: Проверьте настройки базы данных

Убедитесь, что в `.env` правильный `DATABASE_URL`:
```env
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/saferoute
```

### Шаг 5: Запустите бэкенд
```bash
python run.py
```

**НЕ ЗАКРЫВАЙТЕ этот терминал!**

## ✅ Проверка работы

После запуска бэкенда:

1. **Проверьте health endpoint:**
```bash
curl http://localhost:8000/health
# Должен вернуть: {"status":"ok"}
```

2. **Откройте документацию:**
http://localhost:8000/docs

3. **Попробуйте зарегистрироваться:**
http://localhost:5174

## 🔧 Если бэкенд не запускается

### Проблема: greenlet
```bash
pip uninstall greenlet -y
pip install --force-reinstall greenlet
```

### Проблема: База данных не подключена
Убедитесь, что:
- PostgreSQL запущен
- База данных `saferoute` существует
- Правильный `DATABASE_URL` в `.env`

### Проблема: Зависимости
```bash
pip install -r requirements.txt
```

## 📝 Важно!

**Бэкенд должен работать в отдельном терминале!**

После запуска бэкенда регистрация будет работать с реальной базой данных! 🎉





