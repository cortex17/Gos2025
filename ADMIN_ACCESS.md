# 🔐 Как зайти на админ панель

## Способ 1: Быстрый вход (Рекомендуется) ⚡

1. Откройте приложение: `http://localhost:5173`
2. Перейдите на страницу входа: `/login` или нажмите на кнопку "Войти" в хедере
3. Найдите раздел **"Быстрый вход"** внизу формы
4. Нажмите кнопку **"Войти как Админ"** (красная кнопка с иконкой шестеренки)
5. Автоматически произойдет вход и переход на `/admin`

## Способ 2: Через форму входа 📝

1. Откройте страницу входа: `/login`
2. Введите данные:
   - **Email:** `admin@test.com`
   - **Пароль:** `admin123`
3. Нажмите кнопку **"Войти"**
4. После успешного входа перейдите на `/admin` через:
   - Кнопку "Админ" в хедере (если вы вошли как админ)
   - Или напрямую введите URL: `http://localhost:5173/admin`

## Способ 3: Прямой переход (если уже авторизован) 🔗

Если вы уже вошли как админ:
1. Просто перейдите по адресу: `http://localhost:5173/admin`
2. Или нажмите на кнопку **"Админ"** в хедере (красная кнопка справа)

## Тестовые данные для входа

### Админ:
- **Email:** `admin@test.com`
- **Пароль:** `admin123`
- **Роль:** `admin`

### Обычный пользователь:
- **Email:** `student1@test.com`
- **Пароль:** `student123`
- **Роль:** `user`

## Что делать, если не получается зайти?

### Проблема: Кнопка "Войти как Админ" не работает
1. Откройте консоль браузера (F12)
2. Проверьте, есть ли ошибки
3. Попробуйте способ 2 (через форму)

### Проблема: После входа редирект на /dashboard вместо /admin
1. Проверьте в консоли браузера (F12) логи:
   - `[QuickLoginAdmin] Current role in store: admin`
   - `[RequireAdmin] Access granted for admin`
2. Если роль не `admin`, очистите localStorage:
   ```javascript
   localStorage.clear()
   ```
3. Обновите страницу (F5)
4. Попробуйте войти снова

### Проблема: Страница /admin показывает "Access denied"
1. Убедитесь, что вы вошли как админ (проверьте роль в консоли)
2. Очистите localStorage и войдите заново
3. Проверьте, что в localStorage есть:
   - `sr_token` - токен авторизации
   - `sr_role` - должно быть `"admin"`

## Функции админ панели

После входа в админ панель (`/admin`) вы получите доступ к:

### 📋 Вкладка "Модерация"
- Просмотр отчетов на модерации
- Одобрение отчетов (кнопка "Одобрить")
- Отклонение отчетов (кнопка "Отклонить")

### 👥 Вкладка "Пользователи"
- Список всех пользователей
- Просмотр ролей и репутации
- Блокировка/разблокировка пользователей

### 📊 Вкладка "Аналитика"
- Статистика по отчетам
- Карта опасных зон
- Зоны повышенного риска

## Быстрая проверка доступа

Откройте консоль браузера (F12) и выполните:
```javascript
// Проверить текущую роль
console.log("Role:", localStorage.getItem("sr_role"));
console.log("Token:", localStorage.getItem("sr_token") ? "exists" : "missing");

// Быстрый вход как админ (если нужно)
// Это выполнится автоматически при нажатии кнопки "Войти как Админ"
```

---

**Готово!** Теперь вы знаете, как зайти на админ панель. 🎉
