# 🚀 Запуск бэкенда локально

## ⚠️ Текущая проблема
Библиотека `greenlet` установлена неправильно в виртуальном окружении.

## ✅ РЕШЕНИЕ

### Способ 1: Использовать готовый скрипт

Откройте **новый терминал** и выполните:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap"
./fix-and-run-backend.sh
```

**НЕ ЗАКРЫВАЙТЕ этот терминал!** Бэкенд должен работать в нем.

### Способ 2: Вручную исправить greenlet

1. Откройте **новый терминал**
2. Выполните команды:

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
source venv/bin/activate
pip uninstall greenlet -y
pip install --force-reinstall greenlet
python run.py
```

### Способ 3: Пересоздать виртуальное окружение

```bash
cd "/Users/nurdaulettauekel/Desktop/SOSMap/GOS 2025/backend"
rm -rf venv
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python run.py
```

## 🎯 Временное решение: Fake API

**Я уже включил Fake API!** Теперь регистрация будет работать через localStorage.

Чтобы использовать Fake API:
1. Убедитесь, что в `.env` есть: `VITE_USE_FAKE_API=true`
2. Перезапустите фронтенд: `npm run dev`

## ✅ Проверка работы бэкенда

После запуска откройте:
- http://localhost:8000/docs - Swagger документация
- http://localhost:8000/health - должен вернуть `{"status":"ok"}`

## 📝 Важно!

**Бэкенд должен работать в ОТДЕЛЬНОМ терминале!**

Если вы видите:
```
INFO:     Uvicorn running on http://127.0.0.1:8000
```

Значит бэкенд работает! 🎉

